import SwiftUI

/// Punto de entrada de la app (equivalente a MainActivity + setContent en Android).
@main
struct SISCAMobileApp: App {
    var body: some Scene {
        WindowGroup {
            RootView()
        }
    }
}
